context SubseaControlSystem inv:  self.detailedSystemType = DetailedControlTye::DirectHydraulic implies
 self.subseaField->forAll(x:SubseaField|(x.unit = DistanceUnit::KM and x.distance < 15) or 
(x.unit = DistanceUnit::Mile and x.distance < 9))
