context SubseaControlSystem inv:  (self.detailedSystemType= DetailedControlTye::MultiplexedElectrohydraulic) implies
 self.subseaField->forAll(x:SubseaField|(x.unit = DistanceUnit::KM and x.distance > 5) or (x.unit = DistanceUnit::Mile and x.distance > 3))
